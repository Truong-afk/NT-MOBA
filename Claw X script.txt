local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

-- Tạo cửa sổ chính
local Window = Fluent:CreateWindow({
    Title = "NT MOBA",
    SubTitle = " | Tổng Hợp Script Ver1",
    TabWidth = 160,
    Size = UDim2.fromOffset(480, 360),
    Acrylic = false, -- Không dùng hiệu ứng mờ
    Theme = "Dark", -- Giao diện nền đen
    MinimizeKey = Enum.KeyCode.LeftControl -- Phím thu gọn
})

-- Thông báo khởi động
Fluent:Notify({
    Title = "NT MOBA",
    Content = "Chào mấy ní!",
    SubContent = "hihi",
    Duration = 5
})

-- Tạo chức năng thu gọn menu
local isMinimized = false
local menuIcon = Instance.new("ImageButton") -- Biểu tượng menu nhỏ gọn
menuIcon.Image = "rbxassetid://1234567" -- Thay ID này bằng icon bạn muốn
menuIcon.Size = UDim2.new(0, 50, 0, 50)
menuIcon.Position = UDim2.new(0, 10, 0, 10) -- Vị trí trên màn hình
menuIcon.BackgroundTransparency = 1
menuIcon.Visible = false -- Ẩn đi khi chưa thu nhỏ
menuIcon.Parent = game.CoreGui

Window:SetMinimizeCallback(function()
    isMinimized = not isMinimized
    if isMinimized then
        Window.Instance.Visible = false -- Ẩn menu chính
        menuIcon.Visible = true -- Hiện icon nhỏ
    else
        Window.Instance.Visible = true -- Hiện lại menu chính
        menuIcon.Visible = false -- Ẩn icon nhỏ
    end
end)

menuIcon.MouseButton1Click:Connect(function()
    isMinimized = false
    Window.Instance.Visible = true
    menuIcon.Visible = false
end)

-- Tab Scripts Blox Fruits
local Tabs = {
    BloxFruits = Window:AddTab({ Title = "Scripts Blox Fruits", Icon = "rbxassetid://1234567" })
}

Tabs.BloxFruits:AddButton({
    Title = "QuantumOnyx",
    Description = "",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/Trustmenotcondom/QTONYX/refs/heads/main/QuantumOnyx.lua"))()
    end
})

Tabs.BloxFruits:AddButton({
    Title = "REDzHUB",
    Description = "",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/REDzHUB/BloxFruits/main/redz9999"))()
    end
})

Tabs.BloxFruits:AddButton({
    Title = "Fix Lag",
    Description = "",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/TurboLite/Script/main/FixLag.lua"))()
    end
})

-- Tab Youtube Channel
local Tabs = {
    Youtube = Window:AddTab({ Title = "Youtube Channel", Icon = "rbxassetid://1234567" })
}

Tabs.Youtube:AddButton({
    Title = "NT MOBA Channel",
    Description = "",
    Callback = function()
        loadstring(game:HttpGet(("https://www.youtube.com/@ntmoba1265")))()
    end
})

-- Tab Update Information
local Tabs = {
    Update = Window:AddTab({ Title = "Update Information", Icon = "rbxassetid://1234567" })
}

local Section = Tabs.Update:AddSection("Tổng Hợp Nhiều Script Vip Pro Miễn Phí")

-- Kết thúc script
